apiVersion: v1
kind: Service
metadata:
  name: customer-accounts
  labels:
    app: customer-accounts
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: customer-accounts
